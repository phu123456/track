<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <a class="navbar-brand" style="color:white">Logistic</a>
      <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item root" id="root">
            <a class="nav-link">Home<span class="sr-only"></span></a>
          </li>
          <li class="nav-item vehicle" id="vehicle">
            <a class="nav-link">Vehicle</a>
          </li>
          <li class="nav-item tyre" id="tyre">
            <a class="nav-link">Tyres</a>
          </li>
          <li class="nav-item maintenance" id="maintenance">
            <a class="nav-link">Maintenances</a>
          </li>

        </ul>
        <!-- <form class="form-inline mt-2 mt-md-0">
          <input class="form-control mr-sm-2" type="text" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form> -->
        <% if user_signed_in? %>
          <!-- <li> -->
          <div class="dropdown">
            <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown"><i class="	fa fa-user-circle"> <%= current_user.email%></i>
            <span class="caret"></span></button>
            <ul class="dropdown-menu">
              <li align="center"><%= link_to('Logout', destroy_user_session_path, method: :delete) %></li>
            </ul>
          </div>
          <!-- </li> -->
        <% else %>
          <!-- <li> -->
          <%= link_to('Login', new_user_session_path)  %>
          <!-- </li> -->
        <% end %>
      </div>
    </nav>

    <script>
      $('#root').click(function() {
        window.location.assign('<%= root_path %>')
      })

      $('#vehicle').click(function() {
        window.location.assign('<%= vehicles_path %>')
      })

      $('#tyre').click(function() {
        window.location.assign('<%= tyres_path %>')
      })

      $('#maintenance').click(function() {
        window.location.assign('<%= maintenances_path %>')
      })

      $('#root').click(function() {
        window.location.assign('<%= root_path %>')
      })
    </script>

    <style>
      .nav-item {
        cursor: pointer;
      }

      a:link {
          text-decoration:none;
          color: black;
      }

      
    </style>
