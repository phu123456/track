<p id="notice"><%= notice %></p>

<div align="center">
  <input class="btn btn-primary active" type="button" value="Currently Used" id="in-used">
  <input class="btn btn-success" type="submit" value="Available" id="un-use">
  <input class="btn btn-danger" type="submit" value="Destroyed">
</div>
<br>

<style>
  .btn {
      padding: 10px 20px;
      font-size: 18px;
      border-radius: 5px;
      width:20%;
  }

  th
  {
    color:white;
  }
</style>

<table class="table my_table in-used-table">
  <thead>
    <tr bgcolor="#006dcc">
      <th>Brand</th>
      <th>Serial</th>
      <th>Start distance</th>
      <th>Total distance</th>
      <th>Status</th>
      <th>Vehicle</th>
      <th>Position</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tyres.where("tyres.vehicle_id IS NOT NULL").each do |tyre| %>
      <tr class ="in-used-table">
        <td><%= tyre.brand %></td>
        <td><%= tyre.serial %></td>
        <td><%= tyre.start_distance %></td>
        <td><%= tyre.total_distance %></td>
        <td><%= tyre.status %></td>
        <td><%= Vehicle.find(tyre.vehicle_id).plate %></td>
        <td><%= tyre.position %></td>
        <td><%= link_to 'Manage', tyre %></td>
        <td><%= link_to 'Edit', edit_tyre_path(tyre) %></td>
        <td><%= link_to 'Destroy', tyre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<table style="display:none" class="table my_table un-use-table">
  <thead>
    <tr bgcolor="#5cb85c">
      <th>Brand</th>
      <th>Serial</th>
      <th>Start distance</th>
      <th>Total distance</th>
      <th>Status</th>
      <th>Vehicle</th>
      <th>Position</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tyres.where("tyres.vehicle_id IS NULL").each do |tyre| %>
      <tr>
        <td><%= tyre.brand %></td>
        <td><%= tyre.serial %></td>
        <td><%= tyre.start_distance %></td>
        <td><%= tyre.total_distance %></td>
        <td><%= tyre.status %></td>
        <td> - </td>
        <td> - </td>
        <td><%= link_to 'Manage', tyre %></td>
        <td><%= link_to 'Edit', edit_tyre_path(tyre) %></td>
        <td><%= link_to 'Destroy', tyre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Tyre', new_tyre_path %>


<script>
  $('.btn').click(function () {
       $('.btn').removeClass('active');
       $(this).toggleClass('active');
   });

  $('#in-used').click(function () {
    $(".un-use-table").css("display","none");
    $(".drestroyed-table").css("display","none");
    $(".in-used-table").removeAttr( 'style' );
    // $(".in-used-table").css("width", 85%);
  });

  $('#un-use').click(function () {
    $(".in-used-table").css("display","none");
    $(".drestroyed-table").css("display","none");
    $(".un-use-table").removeAttr('style');
    // $(".un-used-table").css("width", 85%);
  });

</script>
<style>
  .my_table {
      width: 85%;
  }
</style>
